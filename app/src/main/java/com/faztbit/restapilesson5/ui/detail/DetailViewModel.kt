package com.faztbit.restapilesson5.ui.detail

import com.deved.weatherapp.coroutines.ScopeViewModel
import kotlinx.coroutines.CoroutineDispatcher
import kotlinx.coroutines.launch

class DetailViewModel(dispatcher: CoroutineDispatcher) : ScopeViewModel(dispatcher) {

    fun xd() {
        launch {

        }
    }
}